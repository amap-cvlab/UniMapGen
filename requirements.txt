# Runtime versions from the original UniMapGen inference environment.
# Install on Linux with CUDA 12.4 and Python 3.10.
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.4.0
torchvision==0.19.0
transformers==4.45.0
tokenizers==0.19.1
safetensors==0.4.5
vllm==0.6.1
qwen-vl-utils==0.0.8

numpy==1.26.4
Pillow==10.4.0
opencv-python-headless==4.10.0.84
scipy==1.14.1
shapely==2.1.0
tqdm==4.66.5

# Mask AP evaluation. pycocotools is used by torchmetrics.
torchmetrics==1.4.1
pycocotools==2.0.8

# Tests and release checks.
pytest==8.3.2

# The validated environment also used flash-attn==2.6.1. Install it after
# torch with: pip install flash-attn==2.6.1 --no-build-isolation
